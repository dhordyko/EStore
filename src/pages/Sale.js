import React, { Fragment, useState, useEffect } from 'react'
//import { getList } from '../components/elements/mist'
const Sale = () => {
    // const [list, setList] = useState([]);
    // useEffect(() => {
    //     let mounted = true;
    //     getList('https://fakestoreapi.com/products/category/jewelery')
    //         .then(items => {
    //             if (mounted) {
    //                 setList(items.map((item, i) => {
    //                     return {
    //                         id: i,
    //                         imageSrc: item.image,
    //                         title: item.title,
    //                         promotion: '',
    //                         price: item.price,
    //                         description: item.description,
    //                     }
    //                 }))
    //             }
    //         })
    //     return () => mounted = false;
    // }, [])

    return (
        <Fragment>
            {/* <ProductList data={list} /> */}
        </Fragment>
    )
}
export default Sale;